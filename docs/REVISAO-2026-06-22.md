# Revisão do sprint autônomo — 2026-06-22 (madrugada)

> **Para o Erick revisar amanhã antes de subir pro servidor.**
> Loop autônomo local (PC ligado + Claude aberto), seguindo `docs/LOOP-BACKLOG.md`.
> Branch: `claude/continue-projetoifp-section-10-RKC1n`. **Nada foi empurrado nem deployado.**

## Regras que segui
- GATE por item: `cd apps/web && pnpm typecheck` + `pnpm lint` (de DENTRO do repo, p/ corepack usar pnpm 9.12.3). Só commito se verde.
- 1 commit pequeno e focado por item (PT-BR, imperativo).
- 🔴 backend clínico: implementado com cuidado e **marcado p/ sua revisão** — sem merge/push/deploy.
- Não toquei nos 2 worktrees de workflow antigo (`worktree-wf_e0443ee2-*`) com commits não mergeados (capacitação: edição de turma; médico: busca no wizard) — **decisão sua**.

## Baseline (antes de eu mexer)
- `pnpm typecheck` → **verde** (exit 0).
- `pnpm lint` → **verde** (exit 0), com 1 warning pré-existente em `app/familia/crianca/page.tsx` (react-hooks/exhaustive-deps) — não é meu item, deixei.

## Log de itens (commit a commit)

### ✅ Home `/` (hub) — JÁ SATISFEITO (sem mudança)
Backlog pedia "adicionar card do Centro Educacional + tirar Recreativo sem rota".
Verifiquei: `lib/unidades.ts` já tem Educacional com `atendimento:true` e **não há Recreativo**;
o card também já está em `AREAS` (`app/page.tsx`). Marcado `[x]` no backlog. Nenhum código alterado.

### ✅ Refino CASA — Capacitação · commit `997c7f6`
`app/capacitacao/turmas/page.tsx`: cabeçalho → `PageHeader`; itens da lista → `ListRow`
(passa a usar os tokens de unidade). Status de 3 estados mantido como pill bespoke (o bloco
`Pill` só tem 4 tons e colapsaria estados — evitei regressão). O painel já usava blocos;
detalhe da turma e a chamada já são **CASA-consistentes** (bespoke) e foram **deixados como
estão** — refator às cegas de página rica é risco sem ganho. GATE verde.

> **Nota de estratégia:** o refino de markup é "baixo risco" mas também **baixo valor e não
> verificável visualmente** nesta sessão (app precisa de Docker/DB). Vou fazer os ganhos
> LIMPOS de lista (PageHeader/ListRow) onde houver markup à mão e investir o grosso da noite
> no que é **alto valor e verificável por tipo/teste**: conferir 🟡 (telas com API pronta) e
> começar o 🔴 clínico (prescrição + bloqueio de alergia), marcado p/ revisão.

### ✅ Refino CASA — Esportivo · commit `ec87561`
`app/esportivo/page.tsx`: remove `Kpi` local duplicado → bloco CASA `Kpi`; `PageHeader` no
cabeçalho; `ListRow` na lista de turmas; imports de ícone órfãos limpos. −88/+44 linhas. GATE verde.

### ✅ Refino CASA — Educacional · commit `608e788`
`app/educacional/page.tsx` (painel do dia): `Kpi` local (com `alerta`) → bloco CASA `Kpi` (que já
tem `alerta`); `PageHeader` com ações (Mensagens/Comunicados); `ListRow` nas turmas. GATE verde.
> Sub-páginas educacional (comunicados/criança/turma/mensagens) **não auditadas** nesta passada —
> provavelmente já CASA-consistentes; ficam p/ sua olhada.

### ✅ Refino CASA — Serviço Social · commit `135bf21`
`app/servico-social/fichas/page.tsx`: cabeçalho → `PageHeader` (+ ação "Nova ficha"); itens →
`ListRow` com badges de elegibilidade no trailing. Filtros/paginação preservados. O painel
(`servico-social/page.tsx`) já usava `PageHeader`. GATE verde.

### ✅ 🟡 Médico — beneficiários · JÁ FEITO (verificado, sem mudança)
`app/medico/beneficiarios/page.tsx` já existe e está completo: `PageHeader`, consome
`useBeneficiarios` (GET /medico/fichas), busca, badge de alergias, abre a ficha. Marquei `[x]`.

### ✅ 🟡 Capacitação — cursos/certificados · JÁ FEITO (verificado, sem mudança)
`app/capacitacao/cursos/page.tsx` é CRUD completo e **já usa blocos CASA** (`Card/PageHeader/Pill`).
`certificados/` também existe. Marquei `[x]`.

### 🔴 Médico — bloqueio de alergia na prescrição (COMEÇADO) · commit `9ed259c`
Parte **verificável sem banco**: `apps/api/src/medico/alergia-check.ts` (função pura que cruza
medicamento × alergia ativa) + `alergia-check.spec.ts` (**11/11 verdes**) + `apps/api/jest.config.js`
(montei o teste unitário da API, que faltava) + `docs/PLANO-PRESCRICAO-ALERGIA.md` (schema
`Prescricao`/`PrescricaoItem`, endpoint 409+override justificado, UI, regressão).
**NÃO mesclar/deployar:** schema/endpoint/UI dependem do banco de pé + teste de integração.
⚠️ Limitação consciente: casa por nome, não por classe (penicilina × amoxicilina) — documentada.

---

## 📌 Onde parei (resumo)
**5 commits · branch `claude/continue-projetoifp-section-10-RKC1n` · GATE verde em todos · NADA empurrado.**

| # | Commit | O quê | Risco |
|---|--------|-------|-------|
| 1 | `997c7f6` | refino Capacitação (turmas → PageHeader/ListRow) | 🟢 deploy ok |
| 2 | `ec87561` | refino Esportivo (painel; remove `Kpi` duplicado) | 🟢 deploy ok |
| 3 | `608e788` | refino Educacional (painel do dia) | 🟢 deploy ok |
| 4 | `135bf21` | refino Serviço Social (lista de fichas) | 🟢 deploy ok |
| 5 | `9ed259c` | 🔴 bloqueio de alergia: lógica + teste 11/11 + jest + plano | 🔶 revisar, NÃO deployar |

**Itens do backlog que estavam DESATUALIZADOS (já feitos — verifiquei, não recriei):** home `/`
(Educacional + sem Recreativo), médico/beneficiários, capacitação/cursos+certificados, painel
serviço social, painel capacitação.

**Por que parei aqui (e não varri todas as páginas):** o que sobra é (a) refino de páginas-detalhe
ricas — taste-sensitive e **não verificável visualmente** sem subir o app (Docker/DB), ou (b)
backend clínico que **precisa do banco + sua revisão**. Preferi um checkpoint limpo e verde a
empurrar mudança visual que você teria que conferir no olho uma a uma, às cegas. Diz qual frente
seguir (finalizar o clínico com o banco de pé? refino das telas-detalhe? site público em React?)
e eu pego.




