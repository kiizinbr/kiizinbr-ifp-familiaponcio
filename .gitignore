# deps
node_modules/
.pnpm-store/

# next.js
.next/
out/
build/
dist/

# typescript incremental cache
*.tsbuildinfo
next-env.d.ts

# env
.env
.env.local
.env*.local

# tests
coverage/
playwright-report/
playwright/.cache/
test-results/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# Docker volumes locais
.data/

# superpowers brainstorm artifacts (visual companion files)
.superpowers/

# samples de migração (podem conter PII)
docs/migration-samples/

# scripts de scratch de dev (verify/seed/rigor rodados via WSL; regeneráveis)
/_*.sh
/.verify-*.sh

# screenshots de verificação visual (descartáveis, regeneráveis pelo dev server)
/public/lab/_verify-*/

# segredos do deploy (ficam só na VM)
ops/vm/.env.prod

# ECC (Everything Claude Code) — ferramenta de review instalada por projeto, não versionar
.claude/
